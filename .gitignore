# Local runtime data (bind mounts)
wine-data
wine-data-spark
wine-data.bak-*
downloads

# Secrets / local overrides
.env

# Wine prefix archive built by prepare-wineprefix.sh
wineprefix.tar.zst
.prepared

# Spark deb deps downloaded by scripts/download-debs.sh
.deb-cache

# Python / build cache
__pycache__

# Node
node_modules

# Deno KV / local runtime data
data
apps/*/data
