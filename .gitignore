# Local runtime data (bind mounts)
wine-data
wine-data-spark
wine-data.bak-*
downloads

# Secrets / local overrides
.env

# Wine prefix archive built by prepare-wineprefix.sh
wineprefix.tar.zst
.prepared

# Python / build cache
__pycache__

# Node
node_modules
